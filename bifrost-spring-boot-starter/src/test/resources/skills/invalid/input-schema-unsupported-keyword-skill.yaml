name: invalid.input.schema.keyword
description: Invalid input schema keyword.
model: gpt-5
input_schema:
  type: object
  properties:
    payload:
      type: string
      oneOf:
        - type: string

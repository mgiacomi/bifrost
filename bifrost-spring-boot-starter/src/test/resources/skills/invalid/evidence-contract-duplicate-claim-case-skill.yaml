name: evidenceContractDuplicateClaimCaseSkill
description: Invalid evidence contract with duplicate claim keys after normalization
model: gpt-5
allowed_skills: [invoiceParser, expenseLookup]
output_schema:
  type: object
  additionalProperties: false
  properties:
    vendorName:
      type: string
  required:
    - vendorName
evidence_contract:
  claims:
    vendorName: invoiceParser
    VendorName: expenseLookup

name: mapped.method.skill
description: YAML skill mapped to a discovered Java implementation.
model: gpt-5
mapping:
  target_id: targetBean#deterministicTarget
input_schema:
  type: object
  additionalProperties: false
  required:
    - input
  properties:
    input:
      type: string

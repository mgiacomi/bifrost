name: mappedMethodSkill
description: YAML skill mapped to a discovered Java implementation with a format mismatch.
mapping:
  target_id: targetBean#deterministicTarget
input_schema:
  type: object
  additionalProperties: false
  required:
    - input
  properties:
    input:
      type: string
      format: date
